import { BusquedaInformacionPipe } from './busqueda-informacion.pipe';

describe('BusquedaInformacionPipe', () => {
  it('create an instance', () => {
    const pipe = new BusquedaInformacionPipe();
    expect(pipe).toBeTruthy();
  });
});
