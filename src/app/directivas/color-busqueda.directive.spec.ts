import { ColorBusquedaDirective } from './color-busqueda.directive';

describe('ColorBusquedaDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorBusquedaDirective();
    expect(directive).toBeTruthy();
  });
});
